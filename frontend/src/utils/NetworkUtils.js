import NetInfo from '@react-native-community/netinfo';
// OfflineLayer automatically handles network status via NetInfo inside the SDK
export const NetworkUtils = {
  init() {
    // Left for backwards compatibility if needed
  }
};
